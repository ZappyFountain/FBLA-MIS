# FBLA MIS Study Tool — Team Dashboard

Upload `index.html` and `questions.js` to the root of the same GitHub Pages repository.

The **Team** tab compares Zain, Ethan, and Yohann using profiles saved in the same browser/device. It does not sync across separate computers because the app uses browser local storage.
